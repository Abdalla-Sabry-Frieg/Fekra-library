@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "التحقق من رمز الاسترداد";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-8 col-lg-6">
            <div class="card shadow-sm p-4">
                <h1 class="text-center mb-4">@ViewData["Title"]</h1>
                <hr />
                <p class="text-center">
                    لقد طلبت تسجيل الدخول باستخدام رمز الاسترداد. لن يتم تذكر تسجيل الدخول هذا حتى تقدم رمز تطبيق المصادقة عند تسجيل الدخول أو تعطل المصادقة الثنائية وتسجيل الدخول مرة أخرى.
                </p>
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="RecoveryCode" />
                        <label asp-for="Input.RecoveryCode" class="form-label">رمز الاسترداد</label>
                        <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
                    </div>

                    <button type="submit" class="w-100 btn btn-lg btn-primary">تسجيل الدخول</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
